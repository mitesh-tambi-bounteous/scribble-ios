# terraform (mirrors partner-pathfinder)
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
# backend.hcl is generated by bootstrap on apply — not committed
environments/*/backend.hcl
# keep .terraform.lock.hcl checked in (provider version pinning)
