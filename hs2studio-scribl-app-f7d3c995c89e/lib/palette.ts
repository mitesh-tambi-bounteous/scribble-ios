/**
 * Shared drawing/avatar color palette — the app's color vocabulary.
 * Used in the draw screen's swatch picker and will be reused in Settings.
 *
 * Canonical source is @scribl/shared/tools; re-exported here so existing
 * import sites keep working.
 */
export { PALETTE } from "@scribl/shared/tools";
