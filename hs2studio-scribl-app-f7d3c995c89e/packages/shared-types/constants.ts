/**
 * Shared runtime constants for the Scribl POC. Unlike the rest of
 * `@scribl/shared`, this file may have actual runtime exports (not just
 * types) for values shared between the client and the mock/http data
 * adapters.
 */
